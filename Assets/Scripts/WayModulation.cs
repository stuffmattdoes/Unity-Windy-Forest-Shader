using UnityEngine;
using System.Collections;

public class WayModulation : MonoBehaviour {

	// Variables
	[Range(0, 10)] public int variationMin = 2;
	[Range(0, 10)] public int variationMax = 8;
	[Range(0, 10)] public int variationSpeed = 5;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
